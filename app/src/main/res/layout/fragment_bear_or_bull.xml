<?xml version="1.0" encoding="utf-8"?>


<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.github.mikephil.charting.charts.BarChart
        android:id="@+id/barChart"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="30dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/red_button" />

    <TextView
        android:id="@+id/bear_or_bull_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:layout_marginEnd="200dp"
        android:text="@string/bear_or_bull_title"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/bear_or_bull_question"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="@string/bear_or_bull_question"
        app:layout_constraintStart_toStartOf="@+id/bear_or_bull_title"
        app:layout_constraintTop_toBottomOf="@+id/bear_or_bull_title" />

    <ImageButton
        android:id="@+id/green_button"
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:layout_marginTop="50dp"
        android:layout_marginEnd="100dp"
        android:background="@color/AliceBlue"
        app:layout_constraintStart_toStartOf="@+id/bear_or_bull_question"
        app:layout_constraintTop_toBottomOf="@+id/bear_or_bull_question"
        app:srcCompat="@drawable/ic_baseline_trending_up_24" />

    <ImageButton
        android:id="@+id/red_button"
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:layout_marginStart="70dp"
        android:background="@color/AliceBlue"
        app:layout_constraintStart_toEndOf="@+id/green_button"
        app:layout_constraintTop_toTopOf="@+id/green_button"
        app:srcCompat="@drawable/ic_baseline_trending_down_24" />
</androidx.constraintlayout.widget.ConstraintLayout>